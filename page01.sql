-- create table products(
--     product_id int,
--     low_fats varchar(1),
--     recyclable varchar(1)
-- );

-- create type y_n as enum(y, n)

-- insert into products(product_id, low_fats, recyclable)
-- values(1, 'y', 'y'),
-- (2, 'n', 'y'),
-- (3, 'n', 'n'), 
-- (4, 'y', 'n'),
-- (5, 'y', 'y'),
-- (6, 'y', 'n');

-- select product_id from products where low_fats = 'y' and recyclable = 'y';

